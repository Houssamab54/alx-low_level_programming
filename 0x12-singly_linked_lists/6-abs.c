#include <stdio.h>
#include "main.h"
/**
 * _abs - Makes the sum of two numbers
 * @n: First operand
 *
 * Return: The sum of the two parameters
 */
int _abs(int n)
{
if (n < 0)
{
return (-n);
}
else
{
return (n);
}
}
