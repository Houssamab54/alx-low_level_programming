#include <stdio.h>
#include "main.h"

/**
 * _islower - Makes the sum of two numbers
 * @c: First operand
 *
 * Return: The sum of the two parameters
 */
int _islower(int c)
{
if (c >= 'a' && c <= 'z')
{
return (1);
}
else
{
return (0);
}
}
